import { PipeReversArrPipe } from './pipe-revers-arr.pipe';

describe('PipeReversArrPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeReversArrPipe();
    expect(pipe).toBeTruthy();
  });
});
